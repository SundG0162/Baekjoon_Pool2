#include <iostream>

using namespace std;

int main()
{
    int input;
    cin >> input;
    if(input == 0)
        cout << "YONSEI";
    else
        cout << "Leading the Way to the Future";
}